name: my_new_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.1.1
  image_picker: ^1.0.7
  flutter_typeahead: ^4.6.2
  http: ^1.3.0
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  latlong2: ^0.9.0
  flutter_map: ^6.0.0  # Remove this if not using OpenStreetMap

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
